最优拟合多边形，用于计算角点数量获取矩形
source4帧率与前几个不同，抽帧处理使视频能同步播放

两点确定一条直线
四点确定两条直线->四点确定两条直线的一个交点
输入四点坐标->计算交点


计算所有斜率之差
以下为仿射变换部分
对点矩阵重排序

两图像叠加

黑色

找出左上角的点，并对坐标重新排序

由于只因视频为60帧，其他视频为30帧，故只因视频每两帧仅取一帧

将La与Lb原地合并后输出到Lc，核心思想是同时遍历La和Lb中的所有字符，将两个列表在当前指针指向的字符作比较，并将较小的字符取出到Lc
La将排在前面
Lb将排在后面
结果将会被输入到该顺序表
